# Todo 리스트 앱 — 단계별 실행 프롬프트

`PRD.md`를 기반으로 작성된 구현 순서다. Step 0부터 순서대로 하나씩
실행하며, 각 Step은 이전 Step 위에 통합되어 항상 실행 가능한 상태를 유지한다.

## Step 0 — 리포지토리 스캐폴딩
현재 저장소에는 관련 없는 LangChain 예제(main.py 등)만 있다. `frontend/`
(React + Vite + TypeScript)와 `backend/`(Node.js + Express) 두 디렉터리를
만들고, 루트에 두 워크스페이스를 한 번에 실행할 수 있는 스크립트(예: 루트
package.json + concurrently, 또는 README에 두 명령어 안내)를 구성한다.
.gitignore에 node_modules, dist 등을 추가한다. README.md를 새 프로젝트
구조에 맞게 갱신한다.
**완료 기준**: `frontend`, `backend` 디렉터리 생성, 각각 최소 설정 파일 존재.

## Step 1 — 프론트엔드 초기화
`frontend/`에 Vite + React + TypeScript 프로젝트를 생성한다. 기본 템플릿을
정리하고, `App.tsx`에 "Todo 리스트" 타이틀만 있는 빈 화면을 렌더링한다.
**완료 기준**: `npm run dev`로 프론트엔드 개발 서버가 뜨고 빈 화면이 보인다.

## Step 2 — 백엔드 초기화 (정적 서빙)
`backend/`에 Express 서버를 만든다. 프론트엔드 빌드 결과(`frontend/dist`)를
정적 파일로 서빙하고, 그 외 라우트는 index.html로 폴백한다(SPA 라우팅 대비).
개발 편의를 위해 루트에서 프론트/백엔드를 함께 실행하는 스크립트를 추가한다.
**완료 기준**: 프론트엔드를 빌드한 뒤 백엔드를 실행하면 브라우저에서 정적
페이지가 보인다.

## Step 3 — 데이터 모델 & localStorage 유틸
PRD §8의 `Todo`, `Tag` 타입을 `frontend/src`에 정의한다. UUID 생성 유틸,
localStorage 읽기/쓰기 유틸(`todo-app:todos`, `todo-app:tags` 키)을
작성한다. 아직 UI에는 연결하지 않아도 되지만, 브라우저 콘솔에서 수동으로
호출해 값이 저장/복원되는지 확인 가능해야 한다.
**완료 기준**: 타입과 storage 유틸 함수가 존재하고 수동 테스트로 동작 확인.

## Step 4 — useTodos 훅 (상태 + localStorage 동기화)
Step 3의 유틸을 사용하는 `useTodos` 커스텀 훅을 작성한다. 최초 로드 시
localStorage에서 복원하고, add/update/remove/toggleComplete 함수를 노출하며,
상태가 바뀔 때마다 localStorage에 자동 저장한다.
**완료 기준**: 훅을 App에서 호출해 콘솔 로그 등으로 CRUD 동작과 저장/복원이
확인된다 (아직 UI는 없어도 됨).

## Step 5 — 할 일 추가 + 목록 표시 UI
`AddTodoForm`(제목 입력 후 추가), `TodoList`/`TodoItem` 컴포넌트를 만들어
Step 4의 훅과 연결한다. 목록이 비어 있을 때 빈 상태 문구를 보여준다.
**완료 기준**: 브라우저에서 할 일을 입력해 추가하면 목록에 즉시 나타나고,
새로고침해도 유지된다.

## Step 6 — 완료 토글 / 수정 / 삭제
`TodoItem`에 완료 체크박스(완료 시 취소선), 인라인 또는 모달 편집, 삭제
버튼(확인 절차 포함)을 추가한다.
**완료 기준**: 완료 토글·수정·삭제가 모두 브라우저에서 동작하고 새로고침
후에도 반영된 상태가 유지된다.

## Step 7 — 태그 데이터 모델 + 태그 관리 UI
Step 3의 `Tag` 타입을 활용해 태그 생성/이름변경/삭제/색상 지정 UI(간단한
관리 패널)를 만들고 localStorage(`todo-app:tags`)와 연동한다.
**완료 기준**: 태그를 만들고 색을 지정하면 목록에 나타나고 새로고침 후에도
유지된다.

## Step 8 — Todo ↔ 태그 연결
`AddTodoForm`/`TodoItem` 편집 화면에 태그 다중 선택 UI를 추가하고, 목록에서
각 할 일에 태그 배지(지정 색상)를 표시한다.
**완료 기준**: 할 일에 태그를 붙이면 배지로 보이고, 태그 삭제 시 연결된
할 일에서도 자연스럽게 제거(또는 무효화)된다.

## Step 9 — 마감일
할 일에 마감일(date picker)을 추가한다. 목록에 마감일을 표시하고, 마감일이
지난 미완료 항목은 시각적으로 강조(예: 빨간 텍스트/배경)한다.
**완료 기준**: 마감일 설정/미설정 모두 정상 동작하고, 기한 초과 항목이
눈에 띄게 표시된다.

## Step 10 — 우선순위
할 일에 우선순위(높음/보통/낮음) 선택 UI를 추가하고, 목록에 색상/아이콘
배지로 표시한다.
**완료 기준**: 우선순위 지정과 표시가 정상 동작한다.

## Step 11 — 검색
제목/설명을 대상으로 하는 검색창을 추가하고, 입력에 따라 목록이 실시간으로
필터링되게 한다.
**완료 기준**: 키워드 입력 시 목록이 즉시 좁혀진다.

## Step 12 — 필터
완료 상태(전체/진행중/완료), 카테고리(태그), 우선순위, 마감일 범위(오늘/
이번주/기한초과)로 필터링하는 컨트롤을 추가하고 검색과 함께 조합 적용되게
한다.
**완료 기준**: 각 필터를 단독/조합으로 적용했을 때 목록이 올바르게
좁혀진다.

## Step 13 — 정렬
마감일순/우선순위순/생성일순/이름순 정렬 옵션을 추가한다.
**완료 기준**: 정렬 옵션 변경 시 목록 순서가 즉시 바뀐다.

## Step 14 — 반응형 & 다듬기
모바일/데스크톱 반응형 레이아웃, 키보드 조작(Enter로 추가 등), 색상 대비
등 접근성을 다듬는다.
**완료 기준**: 좁은 화면(모바일 뷰포트)에서도 모든 기능이 사용 가능하다.

## Step 15 — 최종 점검 & 문서화
전체 기능(§5 CRUD/태그/마감일·우선순위/검색·필터)을 수동으로 다시 점검하고,
README.md에 실행 방법과 기능 설명을 최종 업데이트한다.
**완료 기준**: PRD §4.1 In Scope 표의 모든 항목이 동작하는 것을 확인.
