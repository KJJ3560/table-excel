# 상식 퀴즈 게임 — 3단계 실행 프롬프트

`PRD.md`를 기반으로 작성된 구현 순서다. 3단계로 나누어 순서대로 실행하며,
각 단계는 이전 단계 위에 통합되어 항상 실행 가능한 상태를 유지한다
(PRD §12의 6개 마일스톤을 3단계로 묶었다).

## 1단계 — 문제 데이터 + 기본 퀴즈 플레이 루프

`index.html`, `style.css`, `script.js`, `questions.js`를 저장소 루트에
만든다.

- `questions.js`: 한국사/과학/지리/예술과 문화 4개 카테고리, 각 카테고리당
  실제로 정답이 정확한 4지선다 문제 10개(총 40개)를 작성한다. PRD §8
  데이터 모델을 그대로 따른다(`id`, `category`, `question`, `choices`
  4개, `answerIndex`).
- 시작 화면 없이 우선 40문제를 순서대로 진행하는 기본 루프를 만든다:
  문제 1개 + 보기 4개 표시 → 진행 상황(예: "3 / 40") 표시 → 보기 선택
  즉시 정답/오답 시각적 피드백(정답 강조, 오답 시 정답 위치도 함께
  강조) → 다음 문제로 이동. 각 문제는 한 번만 답변 가능하게 한다.

**완료 기준**: 브라우저에서 `index.html`을 열면 40문제를 순서대로 풀 수
있고, 보기를 고를 때마다 정답 여부가 즉시 보인다.

## 2단계 — 카테고리/전체 선택 모드 + 점수 계산 + 결과 화면

1단계 위에 다음을 추가한다.

- 시작 화면: 한국사/과학/지리/예술과 문화 중 하나(해당 카테고리 10문제만
  진행) 또는 "전체 40문제"를 선택해 시작할 수 있게 한다.
- 정답 개수를 집계하고, 마지막 문제까지 마치면 결과 화면을 보여준다:
  "정답 X / 전체 Y, 정답률 Z%". "다시 하기" 버튼으로 시작 화면으로 돌아갈
  수 있게 한다.

**완료 기준**: 카테고리를 하나 선택하면 그 카테고리 10문제만 진행되고,
"전체"를 선택하면 40문제가 진행된다. 게임이 끝나면 정확한 점수와
정답률이 표시된다.

## 3단계 — 순위 기록(localStorage) + 스타일/반응형 마무리

2단계 위에 다음을 추가한다.

- 게임 종료마다 점수·전체 문제 수·선택한 카테고리(또는 "전체")·일시를
  `localStorage`(`quiz-app:scores`)에 저장한다(PRD §8 `ScoreRecord`
  모델).
- 순위표 화면을 추가한다: 점수 내림차순 상위 N개(예: 10개)를 보여주고,
  카테고리별 순위와 전체 순위를 구분해서 볼 수 있게 한다. 시작 화면에서
  순위표로 진입하는 버튼을 추가한다. 새로운 최고 기록이면 결과 화면에
  "신기록!" 등으로 표시한다.
- 전체 화면 스타일을 다듬는다: 모바일 뷰포트 대응(반응형), 다크모드
  지원 등 이 저장소의 Todo 앱(별도 PR)과 톤을 맞춘다.

**완료 기준**: 게임을 마치면 순위표에 기록이 반영되고, 새로고침해도
순위 기록이 유지된다. 좁은 화면(모바일 뷰포트)에서도 모든 기능이 정상
동작한다.
