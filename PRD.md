# Todo 리스트 앱 — PRD (Product Requirements Document)

## 1. 개요

브라우저에서 사용하는 개인용 Todo 리스트 웹앱을 개발한다. 프론트엔드와
백엔드로 구성된 웹앱 구조를 가지되, MVP 단계에서 할 일 데이터는 서버/DB가
아닌 **브라우저 localStorage**에 저장한다. 백엔드는 정적 프론트엔드 자산을
서빙하는 역할을 담당하며, 이후 서버 저장(DB)으로 확장할 수 있는 구조로
설계한다.

## 2. 목표

- 사용자가 할 일을 빠르게 추가·수정·삭제·완료 처리할 수 있다.
- 할 일을 카테고리/태그로 분류하고, 마감일과 우선순위를 지정할 수 있다.
- 검색과 필터로 원하는 할 일을 쉽게 찾을 수 있다.
- 별도 로그인/서버 DB 없이 개인 브라우저에서 즉시 사용 가능하다.

## 3. 타깃 사용자

- 개인 할 일 관리가 필요한 1인 사용자 (로그인/공유 불필요, MVP 기준).

## 4. 범위 (Scope)

### 4.1 In Scope (MVP)

| 기능 | 설명 |
|---|---|
| 기본 CRUD | 할 일 추가, 제목/설명 수정, 삭제, 완료 처리(체크) |
| 카테고리/태그 | 할 일에 하나 이상의 태그 또는 카테고리 지정, 태그별 색상 구분 |
| 마감일/우선순위 | 마감일(date) 지정, 우선순위(높음/보통/낮음) 지정, 마감일 임박·초과 항목 강조 |
| 검색/필터 | 제목/설명 키워드 검색, 완료 여부·카테고리·우선순위·마감일 범위로 필터링, 정렬(마감일순/우선순위순/생성일순) |
| 데이터 저장 | localStorage에 자동 저장, 새로고침·재접속 시 데이터 유지 |

### 4.2 Out of Scope (MVP 제외, 추후 확장 후보)

- 다중 사용자 로그인/계정, 기기 간 동기화
- 서버 DB 영속 저장 (localStorage → 서버 DB 마이그레이션은 차기 버전 후보)
- 협업/공유 기능 (할 일 공유, 담당자 지정)
- 알림/리마인더 (푸시, 이메일)
- 반복 일정(recurring task)
- 첨부파일, 서브태스크(하위 할 일)

## 5. 기능 요구사항 상세

### 5.1 할 일 CRUD
- 추가: 제목(필수), 설명(선택), 카테고리/태그, 마감일, 우선순위를 입력해 생성.
- 수정: 기존 항목의 모든 필드 인라인 또는 모달 편집.
- 삭제: 확인 절차(예: 삭제 버튼 클릭 시 확인) 후 제거.
- 완료 처리: 체크박스로 완료/미완료 토글, 완료 항목은 목록에서 구분 표시(취소선 등).

### 5.2 카테고리/태그
- 할 일 하나에 태그 여러 개 부여 가능.
- 태그 생성/삭제/이름변경 관리 UI 제공.
- 태그별 색상 지정, 목록에서 태그 배지로 표시.

### 5.3 마감일/우선순위
- 마감일: 날짜 선택기(date picker)로 지정, 미지정 가능.
- 우선순위: 높음/보통/낮음 3단계, 목록에서 색상/아이콘으로 구분.
- 마감일 지난 미완료 항목은 시각적으로 강조(예: 빨간색 표시).

### 5.4 검색/필터
- 검색창: 제목·설명 텍스트 실시간 검색.
- 필터: 완료 상태(전체/진행중/완료), 카테고리, 우선순위, 마감일 범위(오늘/이번주/기한초과 등) 조합 필터.
- 정렬: 마감일순, 우선순위순, 생성일순, 이름순.

### 5.5 데이터 저장
- 모든 변경사항은 즉시 localStorage에 저장(디바운스 적용 가능).
- 앱 최초 로드 시 localStorage에서 데이터 복원, 데이터 없을 경우 빈 목록으로 시작.

## 6. 비기능 요구사항

- 반응형 UI: 데스크톱/모바일 브라우저 모두 지원.
- 성능: 할 일 수백 건 규모에서도 검색/필터가 즉각 반응.
- 접근성: 키보드 조작(추가/완료 토글) 및 명확한 색상 대비.
- 데이터 유실 방지: localStorage 저장 실패 시 사용자에게 알림.

## 7. 기술 스택 (제안)

- 프론트엔드: React + Vite (TypeScript)
- 백엔드: Node.js/Express — 프론트 빌드 결과물 서빙 및 정적 서버 역할(MVP에서는 API 없음, 추후 REST API 확장 지점으로 유지)
- 저장소: 브라우저 localStorage (클라이언트 상태관리는 React state + localStorage 동기화 훅)
- 스타일: CSS Modules 또는 Tailwind CSS (구현 시 결정)

> 백엔드가 필요한 이유는 정적 파일 서빙 및 향후 서버 저장으로의 확장 여지를
> 남기기 위함이며, MVP 자체의 핵심 로직(CRUD/필터/저장)은 100% 프론트엔드에서
> 동작한다.

## 8. 데이터 모델 (초안)

```ts
interface Todo {
  id: string;          // UUID
  title: string;
  description?: string;
  tags: string[];       // Tag id 목록
  dueDate?: string;     // ISO date (YYYY-MM-DD)
  priority: "high" | "medium" | "low";
  completed: boolean;
  createdAt: string;    // ISO datetime
  updatedAt: string;    // ISO datetime
}

interface Tag {
  id: string;
  name: string;
  color: string;        // hex
}
```

localStorage key 예시: `todo-app:todos`, `todo-app:tags`

## 9. 성공 지표 (MVP 기준)

- 할 일 추가부터 완료 처리까지 3클릭 이내 가능.
- 검색/필터 반응 속도 100ms 이내(로컬 데이터 기준).
- 새로고침 후 데이터 유실 없음.

## 10. 열린 질문 / 확인 필요 사항

- 백엔드가 실제로 API를 제공해야 하는지, 아니면 순수 정적 서빙만 하면 되는지.
- 다크모드 지원 여부.
- 향후 서버 DB로 마이그레이션할 계획이 확정적인지 (있다면 데이터 모델에 사용자 ID 등 선반영 필요).

## 11. 마일스톤 (제안)

1. 프로젝트 스캐폴딩 (React + Vite, 백엔드 서버 셋업)
2. 기본 CRUD + localStorage 연동
3. 카테고리/태그 기능
4. 마감일/우선순위 기능
5. 검색/필터/정렬 기능
6. UI 다듬기 및 반응형 대응
