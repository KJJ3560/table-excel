# Todo 리스트 앱 — PRD (Product Requirements Document)

## 1. 개요

하루 10~20개 수준의 할 일을 관리하는 개인용 Todo 앱을 순수 HTML/CSS/
JavaScript로 만든다. 프레임워크나 빌드 도구 없이 `index.html`을 열면
바로 실행되고, 데이터는 브라우저 localStorage에 저장되어 새로고침해도
사라지지 않는다.

## 2. 목표

- 할 일을 빠르게 추가·수정·삭제·완료 처리한다.
- 업무/개인/공부 세 카테고리로 분류한다.
- 전체 대비 완료 진행률을 한눈에 확인한다.
- 설치나 빌드 과정 없이 즉시 사용 가능하다.

## 3. 타깃 사용자

- 하루 10~20개 정도의 할 일을 혼자 관리하는 1인 사용자.

## 4. 범위 (Scope)

### 4.1 In Scope

| 기능 | 설명 |
|---|---|
| 할 일 추가/수정/삭제 | 제목을 입력해 추가, 제목 인라인 수정, 삭제(확인 절차 포함) |
| 완료 체크 | 체크박스로 완료/미완료 토글, 완료 항목은 취소선 등으로 구분 |
| 카테고리 분류 | 업무/개인/공부 중 하나를 지정, 목록에 카테고리 표시 |
| 진행률 보기 | 전체 대비 완료 개수/비율 표시 (예: "3/12 완료 · 25%") |
| 데이터 저장 | localStorage에 자동 저장, 새로고침·재접속 시 유지 |

### 4.2 Out of Scope (이번 범위 제외)

- 카테고리별 필터/탭, 검색, 정렬
- 마감일, 우선순위, 태그 색상 커스터마이즈
- 서버/DB 저장, 로그인, 다중 사용자, 기기 간 동기화
- React 등 프레임워크, 번들러/빌드 도구

## 5. 기능 요구사항 상세

### 5.1 할 일 추가/수정/삭제
- 입력창에 제목을 쓰고 Enter 또는 추가 버튼으로 등록. 빈 제목은 추가되지
  않는다.
- 제목 클릭 또는 편집 버튼으로 인라인 수정.
- 삭제 버튼 클릭 시 간단한 확인 후 제거.

### 5.2 완료 체크
- 각 항목 왼쪽 체크박스로 완료/미완료 토글.
- 완료된 항목은 취소선과 흐린 색상으로 구분 표시.

### 5.3 카테고리 분류
- 할 일 추가 시 업무/개인/공부 중 하나를 드롭다운(또는 버튼 그룹)으로
  선택. 기본값은 "개인" 등 하나로 고정.
- 목록의 각 항목에 카테고리를 배지 또는 라벨로 표시.

### 5.4 진행률 보기
- 화면 상단 또는 목록 위에 "완료 N / 전체 M (P%)" 형태로 표시.
- 진행률 바(간단한 CSS bar)로 시각화.
- 할 일 추가/삭제/완료 토글 시 즉시 갱신.

### 5.5 데이터 저장
- 모든 변경은 즉시 localStorage에 저장.
- 페이지 최초 로드 시 localStorage에서 복원, 데이터 없으면 빈 목록으로
  시작.

## 6. 비기능 요구사항

- 빌드 과정 없이 `index.html`을 브라우저로 열거나 간단한 정적 서버로
  바로 실행 가능해야 한다.
- 할 일 10~20개 규모에서 모든 조작이 즉각 반응해야 한다.
- 데스크톱 우선이되 모바일 브라우저에서도 사용 가능한 최소한의 반응형을
  지원한다.

## 7. 기술 스택

- HTML + CSS + 순수 JavaScript (ES2020+), 프레임워크·번들러 없음.
- 저장소: 브라우저 localStorage.
- 파일 구성: `index.html`, `style.css`, `script.js` 3개 파일, 저장소
  루트에 배치.

## 8. 데이터 모델 (초안)

```js
// Todo
{
  id: string,          // crypto.randomUUID()
  title: string,
  category: "업무" | "개인" | "공부",
  completed: boolean,
  createdAt: string,   // ISO datetime
}
```

localStorage key: `todo-app:todos`

## 9. 성공 지표

- 할 일 추가부터 완료 처리까지 클릭 2~3회 이내.
- 새로고침 후 데이터 유실 없음.
- 진행률 표시가 항상 실제 완료 비율과 일치.

## 10. 확장 후보 (이번 범위 아님, 참고용)

- 카테고리별 필터/탭
- 마감일, 우선순위
- 검색
- 다중 기기 동기화 (서버 저장)

## 11. 마일스톤 (제안)

1. `index.html`/`style.css`/`script.js` 뼈대 + localStorage 유틸
2. 할 일 추가 + 목록 표시
3. 완료 체크 / 수정 / 삭제
4. 카테고리 선택 및 표시
5. 진행률 표시
6. 스타일 마무리 및 반응형 대응
