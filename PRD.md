# 상식 퀴즈 게임 — PRD (Product Requirements Document)

## 1. 개요

한국사·과학·지리·예술과 문화 4개 카테고리, 카테고리당 10문제씩 총 40문제로
구성된 4지선다 객관식 상식 퀴즈 게임이다. 문제를 풀 때마다 정답/오답을
즉시 피드백하고, 게임이 끝나면 최종 점수와 순위(최고 기록)를 보여준다.
브라우저에서 바로 실행되는 클라이언트 전용 앱으로 만든다.

## 2. 목표

- 40문제를 끊김 없이 즐겁게 풀 수 있다.
- 매 문제마다 정답 여부를 즉시 알 수 있다.
- 점수와 최고 기록 순위를 통해 재도전 동기를 준다.

## 3. 타깃 사용자

- 상식 퀴즈를 가볍게 즐기려는 개인 사용자.

## 4. 범위 (Scope)

### 4.1 In Scope

| 기능 | 설명 |
|---|---|
| 4지선다 객관식 | 문제마다 보기 4개 중 하나를 선택 |
| 카테고리 | 한국사/과학/지리/예술과 문화, 각 10문제(총 40문제) |
| 카테고리 선택 | 시작 화면에서 카테고리 하나(10문제) 또는 전체(40문제)를 선택해 시작 |
| 즉시 피드백 | 보기 선택 즉시 정답/오답 표시(정답 강조, 오답 시 정답도 함께 표시) |
| 최종 점수 | 게임 종료 시 "정답 X / 전체 Y, 정답률 Z%" 표시 |
| 순위 기록 | 점수·카테고리·일시를 저장해 최고 기록 목록(순위표)으로 표시 |
| 데이터 저장 | 순위 기록은 localStorage에 저장, 새로고침해도 유지 |

### 4.2 Out of Scope (이번 범위 제외)

- 서버 기반 전체 사용자 공용 순위(글로벌 리더보드), 로그인/계정
- 문제 편집/관리자 CMS (문제는 코드에 하드코딩된 고정 데이터셋)
- 문제별 제한시간(타이머)
- 멀티플레이어, 오답노트/다시풀기, 난이도 단계

## 5. 기능 요구사항 상세

### 5.1 시작 화면
- 카테고리 4개(한국사/과학/지리/예술과 문화) 중 하나 선택, 또는 "전체
  40문제" 선택 후 시작.
- 순위표(최고 기록 목록) 진입 버튼 제공.

### 5.2 퀴즈 진행
- 한 번에 문제 하나와 보기 4개를 표시, 진행 상황(예: "3 / 10") 표시.
- 보기 선택 시 즉시 정답 여부를 시각적으로 표시(정답: 초록 강조, 오답:
  빨강 강조 + 정답 위치도 함께 강조).
- 피드백 후 "다음 문제" 버튼 또는 짧은 지연 후 자동으로 다음 문제로 이동.
- 각 문제는 한 번만 답변 가능(재선택 불가).

### 5.3 결과 화면
- 게임 종료 시 최종 점수("정답 X / 전체 Y", 정답률 %) 표시.
- 새로운 최고 기록이면 표시(예: "신기록!").
- "다시 하기", "순위표 보기" 버튼 제공.

### 5.4 순위 기록
- 게임 종료마다 점수·카테고리·일시를 localStorage에 저장.
- 순위표는 점수 내림차순으로 상위 N개(예: 10개)를 표시.
- 카테고리별 순위와 전체(40문제) 순위를 구분해서 볼 수 있다.

## 6. 비기능 요구사항

- 빌드 도구 없이 `index.html`을 열면 바로 실행 가능해야 한다.
- 보기 선택 후 피드백까지 지연이 체감되지 않아야 한다(즉시).
- 데스크톱 우선이되 모바일 브라우저에서도 사용 가능한 반응형을 지원한다.

## 7. 기술 스택

- HTML + CSS + 순수 JavaScript(ES2020+), 프레임워크·번들러 없음.
- 문제 데이터: 자바스크립트 배열/객체로 하드코딩(별도 파일, 예: `questions.js`).
- 저장소: 브라우저 localStorage(순위 기록).
- 파일 구성: `index.html`, `style.css`, `script.js`, `questions.js`,
  저장소 루트에 배치.

## 8. 데이터 모델 (초안)

```js
// Question
{
  id: string,
  category: "한국사" | "과학" | "지리" | "예술과 문화",
  question: string,
  choices: [string, string, string, string], // 보기 4개
  answerIndex: number, // 정답 인덱스 0~3
}

// ScoreRecord (순위 기록)
{
  id: string,
  score: number,
  total: number,
  category: string, // 선택한 카테고리 이름 또는 "전체"
  playedAt: string, // ISO datetime
}
```

localStorage key: `quiz-app:scores`

## 9. 성공 지표

- 40문제(또는 선택한 카테고리 10문제)를 끝까지 끊김 없이 완주 가능.
- 정답 선택부터 피드백 표시까지 체감 지연 없음.
- 새로고침 후에도 순위 기록이 유지됨.

## 10. 확인 필요 / 가정

- 카테고리 선택(10문제) + 전체(40문제) 두 모드를 모두 제공한다고
  가정했다. 카테고리 선택 없이 40문제를 고정 순서로만 진행하길
  원하시면 알려주세요.
- 실제 문제/보기 40개 콘텐츠는 이번 PRD에는 포함하지 않았다 — 구현
  단계에서 별도로 채워야 한다.
- 제한시간(타이머)은 요청에 없어 이번 범위에서 제외했다 (§11 확장
  후보 참고).
- 순위 기록은 이 브라우저에서만 유지되는 로컬 기록이며, 서버 공유는
  하지 않는다고 가정했다.

## 11. 확장 후보 (이번 범위 아님)

- 문제별 제한시간
- 서버 기반 글로벌 리더보드
- 오답노트/다시풀기
- 난이도 단계, 문제 추가/편집 도구

## 12. 마일스톤 (제안)

1. `index.html`/`style.css`/`script.js`/`questions.js` 뼈대 + 문제
   데이터 구조(카테고리당 10문제, 총 40문제)
2. 시작 화면(카테고리/전체 선택) + 퀴즈 진행 화면(문제·보기 표시)
3. 정답/오답 즉시 피드백
4. 점수 계산 + 최종 결과 화면
5. 순위 기록(localStorage) 저장 및 순위표 화면
6. 스타일 마무리 및 반응형 대응
